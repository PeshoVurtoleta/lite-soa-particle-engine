/**
 * T2 -- lane view / aliasing matrix. RESERVED, registered empty.
 *
 * Filled by S5, when the seven lanes become `subarray` views into one
 * ArrayBuffer. Registered now (no-op run) so S5 extends a slot instead of
 * renumbering the suite -- exactly as lite-bvh registers its reserved tier empty.
 */
export function run() {}
