/**
 * T5 -- differential fuzz against an AoS oracle. RESERVED, registered empty.
 *
 * Filled by S3, when tick(dt) exists so the SoA core and a plain array-of-structs
 * oracle can be driven through the same dt sequence and compared. Registered now
 * (no-op run) so S3 extends a slot instead of renumbering the suite.
 */
export function run() {}
